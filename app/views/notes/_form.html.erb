

<%= form_for line.notes.build do |f| %>
<%= f.hidden_field :line_id %>
<%= f.text_area :name, class:'form-control' %><br>
<%= f.collection_select :user_id, User.all, :id, :username, {prompt: 'Select User'}, {:class => 'form-control'} %><br>
<%= f.submit class:'form-control' %>
<% end %>
