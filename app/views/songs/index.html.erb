<div class="jumbotron">
<% @artists.each do |artist| %>
<h3 class="display-4"><%= artist.name %></h3>
<% artist.songs.order(:name).each do |song| %>
    <p class="lead"><%= link_to song.name, song_path(song) unless song.lines.length == 0 %></p>
    <% end %>
    <hr>
    <% end %>
</div>
