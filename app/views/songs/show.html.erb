<%= link_to 'Back to songs', songs_path %>
<div class="jumbotron">
    <h1 class="display-3"><%=@song.name%></h1>
    <% @song.lines.each do |line| %>
    <p class="lead"><%= line.name%></p>
    <% end %>
</div>